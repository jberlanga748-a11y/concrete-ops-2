services:
  concrete-ops:
    build:
      context: .
    image: concrete-ops:local
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      DATA_DIR: /app/data
      SEED_DEMO_DATA: "true"
    volumes:
      - ./data:/app/data
